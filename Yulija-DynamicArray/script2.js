const getCoins = (sum, coins) => {
    const result = [];

    return result;
};

const main = () => {
    const penny = [1, 3, 4, 2];
    const amount = 5;
    res = getCoins(amount, penny);
    console.log(res);
};

main();